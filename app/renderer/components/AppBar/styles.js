// Styles
import { appBarMargin } from '../../assets/jss/material-kit-react';
import makeStyles from "@material-ui/core/styles/makeStyles";

const useStyles = makeStyles({
    appBar: {
        ...appBarMargin,
    }
})

export default useStyles;